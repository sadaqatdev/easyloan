<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             Flutter draws its first frame -->
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>

    <style name="AppCompatTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">?android:colorBackground</item>
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
    </style>

    <!--Dialogtheme-->
    <style name="HDialog" parent="@style/Theme.AppCompat.Dialog">
        <!---->
        <!--<item name="android:windowBackground">@color/dialog_transparent</item>-->
        <!---->
        <item name="android:windowNoTitle">true</item>
        <!---->
        <item name="android:windowFrame">@null</item>
        <!--activity-->
        <item name="android:windowIsFloating">true</item>

    </style>

    <!--     -->
    <!--     -->
    <style name="hc_dialog_default" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_default_show</item>  <!---->
        <item name="android:windowExitAnimation">@anim/dialog_default_cancel</item> <!---->
    </style>
    <!--     -->
    <style name="hc_dialog_scale" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_scale_show</item>  <!---->
        <item name="android:windowExitAnimation">@anim/dialog_scale_cancel</item> <!---->
    </style>

    <!--    left -->
    <style name="hc_dialog_translate_left" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_left_show</item>
        <item name="android:windowExitAnimation">@anim/dialog_left_cancel</item>
    </style>

    <!--    top -->
    <style name="hc_dialog_translate_top" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_top_show</item>
        <item name="android:windowExitAnimation">@anim/dialog_top_cancel</item>
    </style>

    <!--    right -->
    <style name="hc_dialog_translate_right" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_right_show</item>
        <item name="android:windowExitAnimation">@anim/dialog_right_cancel</item>
    </style>

    <!--    bottom -->
    <style name="hc_dialog_translate_bottom" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_bottom_show</item>
        <item name="android:windowExitAnimation">@anim/dialog_bottom_cancel</item>
    </style>
</resources>
